#!/usr/bin/env bash
# git pre-commit hook: runs a headless code review via local Ollama model.
# Enable with: git config core.hooksPath .githooks
set -euo pipefail

REPO_ROOT="$(git rev-parse --show-toplevel)"
exec "$REPO_ROOT/.githooks/pre-commit-review.sh"
